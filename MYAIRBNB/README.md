# Proyecto Base Next.js 16

Comando de inicializacion solicitado:

`npx create-next-app@latest . --ts --tailwind --eslint --app --src-dir --import-alias "@/*"`

## Ejecutar

1. Instalar dependencias:
	`npm install`
2. Modo desarrollo:
	`npm run dev`